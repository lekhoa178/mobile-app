import { StyleSheet, View, Text } from 'react-native';
import { FontAwesome } from '@expo/vector-icons';

function Volume() {
  return <FontAwesome name='volume-up' size={35} color={'#7599FF'} />;
}

export default Volume;

const styles = StyleSheet.create({});
